(setq package-enable-at-startup nil)

;; set fonts and frame settings
(setq default-frame-alist `(
			    (font . "CodeNewRoman Nerd Font Mono-12")
			    (fullscreen . maximized)
			    (undecorated . nil)
			    (vertical-scroll-bars . nil)
			    (horizontal-scroll-bars . nil)
			    (menu-bar-lines . 0)
			    (tool-bar-lines . 0)
			    ))
